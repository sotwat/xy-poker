// This component is currently deprecated as we switched to Global In-Page Push (index.html)
// Conditional logic (PC Only) is handled in index.html

import React from 'react';

const MonetagBanner: React.FC<{ width?: number; height?: number }> = () => {
    return null;
};

export default MonetagBanner;
