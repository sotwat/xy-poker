# XY-GTO-A8：終盤の優越関係を証明してから着手を変える

2026年9月5日、v09051209。A7以降の40章の研究履歴を確認し、全列挙できる終盤の検証へ進んだ。ゲーム内AIの既定値をA8へ更新し、保証付きの終盤置換を採用した。序中盤の評価重みとロールアウトはA7を維持する。

## 採用する原則

**相手の伏せ札をどう推定しても悪化しない手があるなら、A7の事前評価よりその証明を優先する。**

A7は64個の仮想世界による終局評価と、20%の静的評価を混ぜて着手を選ぶ。小さな勝敗差を64標本で見落としたり、静的評価が確実な勝ちを取り消したりする場合がある。終盤で全候補を調べ終えられるなら、その部分に近似を残す必要はない。

ただし、仮想世界ごとに別の手を選ぶことは許さない。現実に選ぶ一つの手を、すべての仮想世界に対して検証する。

## 二つの証明条件

| 局面 | 置換を認める条件 |
|---|---|
| 両者の最後の配置のうち、後の一手 | 全伏せ札配置でA7以上の勝敗となり、少なくとも一配置では改善する |
| 自分の最終配置後、相手に一手残る | 相手の伏せ札と最後に置く札の全組合せに対して勝利が確定する |

後の一手について、A7の選択を `a₀`、代替手を `a`、未知の盤面を `h`、勝ち・引き分け・負けの効用を `u = +1, 0, −1` とする。

`u(a, h) ≥ u(a₀, h)  （すべての h）`

が成り立ち、一部の `h` で厳密に大きければ、どの信念分布で平均しても悪化しない。判定に学習済みの伏せ札分布は要らない。

相手に一手残る場合は、単に同じ応答に対して優越するだけでは不十分である。こちらが公開する札により、相手の選択自体が変わり得る。そこで、全応答に勝てる場合だけ置換する。実際には相手の手札にない札も応答候補に含めるため、見逃しはあり得るが、その広い集合で確認した勝利保証は弱くならない。

これは有限の終盤についての保証であり、ゲーム全体のナッシュ均衡を求めた結果ではない。相手の全戦略に対するexploitabilityの値も計算していない。全ゲームの戦略を考慮せず、部分ゲームの解をそのまま移植できない点は、[Brown・Sandholmの部分ゲーム解法](https://papers.nips.cc/paper_files/paper/2017/hash/7fe1f8abaad094e0b5cb1b01d712f708-Abstract.html)とも区別する必要がある。本実装は均衡再計算の代わりに、終端の点ごとの優越関係、または全応答への勝利を確認する。

## 具体例：Qを置く引き分けから、4を置く確実な勝ちへ

出目は `5,4,4,3,1`。最後の列の上二枚はJ・Q、X役の既存四枚は6・5・8・7だった。

A7はQを置き、実際の対局では引き分けになった。代わりに4を置くとX役が4〜8のストレートになる。Y役だけを見ると弱くなるが、ゲーム全体では、残っている伏せ札の**全6,840通りで勝ち**になる。A8はこの4を選ぶ。

この対局由来の状態を [`endgame.json`](src/logic/fixtures/endgame.json) に保存した。別の、相手の応答にかかわらず勝てる状態も [`endgame-reply.json`](src/logic/fixtures/endgame-reply.json) に保存している。

## 独立した終盤検証

探索用とは別のシード `10826561` で2,000局を生成した。終盤までの棋譜生成には軽量A7を用い、比較対象となる最終手は本番と同じ1,000ms・64標本のA7で決めた。各局から最後の二手を取り出し、それぞれ一方だけを置換して結果を比較した。相手に一手残る場合は、変更後の公開状態から相手の本番A7を再実行した。

| 対象 | 局面数 | 証明による置換 | 実際の勝敗改善 | 悪化 | 平均効用差と95%区間 |
|---|---:|---:|---:|---:|---|
| 最後の一手 | 2,000 | 30 | 6 | 0 | +0.0035［+0.00056, +0.00644］ |
| 相手に一手残る | 2,000 | 9 | 5 | 0 | +0.0035［+0.00025, +0.00675］ |

同じ局からの二局面なので、4,000件を独立した対局とは扱わない。表の区間は段階ごとに、一局一観測で計算した。勝敗が変わらなかった置換も、その時点では知らない他の伏せ札配置に対して改善を持つ。

証明計算の平均時間は最後の一手で8.47ms、相手に一手残る局面で107.33ms。最大はそれぞれ28.34ms、410.42msだった。この時間は端末依存である。実行時はA7の探索と同じ1,000msの締切を共有し、列挙が終わらなければ証明を破棄する。

本番A7同士が終盤まで探索した別の12局、24終盤局面でも確認したが、置換は0回だった。したがって、上の改善率を本番A7同士の対戦勝率へ読み替えることはできない。現時点で示せるのは、保証付きで取りこぼしを回収できることと、その頻度が小さいことである。

## 調べたが、採用の根拠にはしなかった方式

### 伏せ札の選択傾向を学習する方式

軽量A7による4,000局では、完成Y役の59.36%が純正ストレート以上だった。無作為な順序付き3枚では1.54%であり、伏せ札を一様に置き直す推定には大きな偏りがある。

出目、配置段、伏せ位置、役の種類とキッカーから列ごとの尤度比を学習した。独立200局でカード識別のBrier誤差は0.9684から0.7875へ下がった。本番探索による別8局でも0.9720から0.9158へ下がった。ただし、全手番へ適用した試作は、1,000msの席交換12組24試合で10勝12敗2分け、平均効用−0.0833となった。予測改善を対戦強化と同一視せず、実行時の学習済み推定器は採用しなかった。

公開された行動から信念を更新する考え方自体には、[Bayes’ Bluff](https://arxiv.org/abs/1207.1411)などの先行研究がある。今回の列ごとの因子モデルは行動履歴全体や列間の構築依存を表しておらず、完全なベイズ事後分布ではない。

### 読みの中の最終手も改善する方式

全手番のロールアウト中で、Y得点差とX実点差の下限から、確実に劣る最終配置を置換する候補も作った。個々の仮想対局の改善は証明できても、それを使って選び直した序中盤の手の強さは別途検証が必要である。

予備24試合は12勝11敗1分け、平均効用+0.0417だった。独立48配札組96試合では53勝40敗3分け、平均効用+0.1354、95%区間は−0.0765～+0.3473だった。平均得点差は+0.8646。両者とも一手1,000ms・64標本で、四つの独立シード`10826571～10826574`を各12配札組ずつ比較した。

点推定は正だが、事前条件の「95%信頼下限が0を超える」を満たさなかった。読みの内部を変更するコードは除去し、保証の成立する実際の終盤着手だけを採用した。53勝40敗という結果を、採用したA8の戦績として引用してはならない。

## 再現と検査

```bash
node --import tsx scripts/analyze_endgame.ts \
  --games=2000 --seed=10826561 --output=/tmp/xy-endgame-confirmation.json

node --import tsx scripts/analyze_endgame.ts \
  --search --games=12 --seed=10826542 --output=/tmp/xy-endgame-search.json

node --import tsx --test src/logic/endgame.test.ts
npm run check
```

`npm run check`で全70テスト、lint、型検査、製品ビルドが成功した。単体テストは、証明器とは独立したゲーム本体の `gameReducer` に全伏せ札配置を渡し、全候補の勝敗・優越関係を照合する。応答が残る局面も全札を列挙して勝利を確認する。双方のロイヤルフラッシュ、先後の優先規則、非公開札の置換不変性、時間切れによる証明破棄を含む。

集計は [`gto_a8_endgame_analysis.json`](gto_a8_endgame_analysis.json) に保存した。実測の改善と、終端での数学的保証、未検証の全体強度を区別して読む。
